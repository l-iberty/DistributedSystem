[0]

# 该文件只有第一行会被读取.

# 当candidate成为leader后, 调用`appendLog()'读取该文件并附加新的日志项.
# 列表[]的长度表示每次附加多少个日志项, 列表中的数字表示日志任期(term),
# 附加日志项时会用currentTerm加上列表中定义的初始任期, 例如: 假设节点的
# currentTerm=1, 列表为[0], 那么将会附加1个日志项, 其任期为1; 如果列表
# 为[0,0,0], 将会附加3个日志项, 任期均为1
